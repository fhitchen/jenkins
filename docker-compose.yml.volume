version: '2'

services:
  jenkins:
    build: 
      context: ./jenkins
      args:
        - http_proxy
        - https_proxy
        - no_proxy
    ports:
      - "9080:8080"
    volumes:
      - jenkins_home:/var/jenkins_home

volumes:
  jenkins_home:
    external:
      name: jenkins_home 
